# 🗺️ XStates

A React app for selecting Country → State → City using API data.

## 🚀 Features
- Fetches data from:
  - `/countries`
  - `/country={country}/states`
  - `/country={country}/state={state}/cities`
- Dropdowns enable step by step
- Shows message: “You selected City, State, Country”
- Responsive layout:  
  - 1 line on desktop  
  - stacked on mobile
- Logs errors with: `Error fetching data: <error>`

## 🛠️ Setup
- npm install
- npm start
